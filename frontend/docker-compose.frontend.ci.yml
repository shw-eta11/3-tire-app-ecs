version: '3.8'

services:
  backend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    image: frontend:${IMAGE_TAG}
    env_file:
      - ./frontend/.env
    ports:
      - "3000:3000"
    restart: always

version: '3.8'

services:
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    image: backend:${IMAGE_TAG}
    env_file:
      - ./backend/.env
    ports:
      - "4000:4000"
    restart: always
